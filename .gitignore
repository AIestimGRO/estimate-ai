# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Secrets
.env

# OS
.DS_Store
Thumbs.db

# Real client data — never commit actual client BOQ/price files,
# only anonymized fixtures explicitly added to tests/fixtures/
data/real/
data/*.xls
data/*.xlsx
data/*.db
*.xlsx.bak

# Stray pytest cache directory variant created in some sandboxes
pytest-cache-files-*/

# Cursor local
.cursor/cache/

# Local throwaway diagnostics on data/real/ (not maintained; similar tools
# like scripts/compare_6356153.py stay tracked when promoted)
scripts/check_load.py
scripts/check_raw.py
scripts/compare_6458203.py
scripts/debug_row101.py
scripts/inspect_headers.py
scripts/verify_out.py

# Admin shell delivery artifacts (apply once, then ignore)
admin-shell.patch
admin-shell-changed-files.zip
_admin_shell_review/
